package user.manager.command.side.domain.aggregate.model.user;

public class IdentifierTest {

	/*@Test
	public void testOK() {
		Identifier identifier = new Identifier("id");
		
		assertNotNull(identifier.getId().equals(1L));
	}
	
	@Test(expected = IllegalArgumentException.class)
	public void testIllegalArgumentException() {
		new Identifier(-1L);		
	}
	
	@Test
	public void testInmutability() {
		Identifier identifier1 = new Identifier(1L);
		
		Identifier identifier2 = new Identifier(identifier1.getId()); 
		
		assertTrue(identifier1 != identifier2);
		assertTrue(identifier1.getId() != identifier2.getId());
	}
	
	@Test
	public void testClone() throws CloneNotSupportedException {
		Identifier identifier1 = new Identifier(1L);
		
		Identifier identifier2 = (Identifier) identifier1.clone(); 
		
		assertTrue(identifier1 != identifier2);
		assertTrue(identifier1.getId() != identifier2.getId());
	}*/

}
